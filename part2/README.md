🧪 Testing & Validation

This project was manually tested using cURL to verify that all API endpoints behave as expected, return correct status codes, and follow the defined input/output formats.

🔹 Prerequisites
	•	Python 3
	•	Flask & Flask-RESTx
	•	Dependencies installed via:
  ```bash
pip install -r requirements.txt

🔹 Running the Server
```bash
cd holbertonschool-hbnb/part2
python3 run.py
